# 程式總結文件

## 檔案結構

```
├── phase1_generate.py      # Phase 1 主程式
├── phase2_compose.py       # Phase 2 主程式（未修改）
├── settings.yaml           # 設定檔
└── core/
    ├── parser.py           # PPT 解析（未修改）
    ├── script.py           # 講稿組合
    ├── tts.py              # TTS 配音
    ├── video.py            # 影片合成
    └── utils.py            # 共用工具（未修改）
```

---

## phase1_generate.py

**功能**：Phase 1 主程式入口，依序執行 A→B→C 三個階段。

**執行方式**：
```bash
python phase1_generate.py --pptx file.pptx
python phase1_generate.py --pptx file.pptx --pages 1-3,5
python phase1_generate.py --pptx file.pptx --range 58,60-65
python phase1_generate.py --pptx file.pptx --workers 8
python phase1_generate.py --pptx file.pptx --no-gpu
```

**CLI 參數**：

| 參數 | 預設值 | 說明 |
|------|--------|------|
| `--pptx` | 必填 | PPT 檔案路徑 |
| `--settings` | `settings.yaml` | 設定檔路徑 |
| `--clips` | `clips` | clips 輸出資料夾 |
| `--pages` | 空（全部） | 指定頁碼，如 `1-3,5` |
| `--range` | 空（全部） | 指定標題編號，如 `58,60-65` |
| `--workers` | `4` | Phase C 平行合成數 |
| `--no-gpu` | False | 停用 GPU，改用 CPU libx264 |

**函式**：

`load_settings(path)` — 讀取 yaml 設定檔，回傳 dict。

`write_scripts_txt(scripts, parsed_list, target_indices, output_path)` — 將本次處理頁的講稿輸出為 `clips/scripts.txt` 預覽檔，段落間用 ` / ` 分隔，截前 200 字。

`main()` — 主流程：
- 載入設定 → 解析 PPT → 組合講稿 → 計算目標頁
- Phase A：只截目標頁截圖（`export_slides_to_images`）
- Phase B：只為目標頁生成 TTS 音檔（`generate_audio_for_targets`）
- Phase C：過濾已存在/音檔缺失的頁，平行合成 MP4（`create_clips_parallel`）

---

## core/script.py

**功能**：將 `parser.py` 的解析結果依 settings 規則組合為段落列表，每頁回傳 `list[str]`，每個元素為一個純文字段落（無 SSML），停頓由 `tts.py` 用靜音音檔實現。

**函式**：

`_normalize_subtitle(subtitle)` — 去除副標題結尾的頁碼後綴，支援 `(2/3)`、`（2/3）` 等格式。例：`"PowerShell設定方法(2/3)"` → `"PowerShell設定方法"`。

`get_subtitle_config(subtitle, settings)` — 取得副標題對應的設定 dict。比對順序：完全匹配 → 正規化後前綴匹配 → fallback 到 `default`。解決多頁副標題如 `(1/3)(2/3)` 無法 match 的問題。

`_parse_notes_indices(settings)` — 將 settings 的 `notes_slides` 解析為 0-based index 集合。

`build_table_sentence(table_rows, settings)` — 將表格資料轉為自然語句，依 `table_supported_symbols` 判斷支援/不支援的方法。

`build_script(parsed, is_first_page, is_continuation, is_last_page_of_subtitle, settings, read_notes)` — 組合單頁講稿，回傳 `list[str]`。所有 append 統一走內部 `_add()` 函式，過濾 None 和空字串，防止無效文字傳入 TTS。

`build_all_scripts(parsed_list, settings)` — 遍歷所有投影片，回傳 `list[list[str]]`。`is_continuation` 和 `is_last` 的判斷改用正規化後的副標題，`subtitle_last_idx` 的 key 也用正規化副標題，確保多頁副標題的續頁/收尾邏輯正確。

---

## core/tts.py

**功能**：將段落列表轉為整頁音檔。每頁逐段生成 mp3 → 段落間插入靜音 → ffmpeg 串接。支援 Semaphore 限流、遞增重試、斷點續跑、debug log。

**函式**：

`_init_debug_log(base_dir)` — 在 `base_dir/debug/` 建立本次執行的 log 檔（含時間戳），回傳路徑。沒有錯誤時執行結束會自動刪除。

`_write_debug(log_path, message)` — 將錯誤訊息附加寫入 log 檔。

`_generate_silence(path, duration_ms, ffmpeg_path)` — 用 ffmpeg 生成靜音 mp3，已存在則跳過（可重複使用）。

`_generate_one(text, path, voice, sem, log_path, page_1based, seg_idx, retries=3)` — 生成單段落音檔。透過 `sem`（Semaphore）限制同時請求數，失敗後等待時間遞增（2s→4s→6s），全部失敗後寫入 debug log，印出頁碼、段落編號、段落內容。

`_concat_audio(segment_paths, out_path, ffmpeg_path)` — 用 ffmpeg 將段落音檔與靜音交錯串接為整頁音檔。

`_generate_page_audio(parts, page_audio_path, page_idx, voice, silence_path, seg_dir, ffmpeg_path, sem, log_path)` — 整頁音檔生成主流程。空頁複製靜音、單段直接生成、多段逐段生成後串接。

`generate_audio_for_targets(scripts, target_indices, audio_dir, settings, clips_dir="")` — 批次生成入口。建立 Semaphore、靜音檔、debug log，分批（batch_size）並發生成，tqdm 進度條顯示整頁完成進度，已存在的整頁音檔跳過（斷點續跑）。

**settings 對應參數**：

| 參數 | 預設值 | 說明 |
|------|--------|------|
| `tts_voice` | `zh-TW-HsiaoChenNeural` | TTS 語音 |
| `tts_batch_size` | `20` | 每批次並發頁數 |
| `tts_max_concurrent` | `5` | 同時進行的 TTS 請求上限（Semaphore） |
| `pause_between_paragraphs_ms` | `300` | 段落間靜音長度（毫秒） |
| `ffmpeg_path` | `ffmpeg` | ffmpeg 執行檔路徑 |

**debug log 位置**：`clips/debug/tts_errors_YYYYMMDD_HHMMSS.txt`，格式：
```
[TTS 失敗] 第 10 頁 段落 4
  檔案：page_0010_seg_004.mp3
  錯誤：No audio was received...
  內容（前200字）：'以上為本原則的說明。'
```

---

## core/video.py

**功能**：截圖匯出、單頁 MP4 合成、ffmpeg 串接。移除 moviepy 依賴，直接呼叫 ffmpeg，支援 NVIDIA GPU 加速與平行合成。

**函式**：

`export_slides_to_images(pptx_path, image_dir, target_pages, dpi=200)` — 透過 PowerPoint COM 只截 `target_pages`（1-based 頁碼 list）的截圖，用 `Slide.Export()` 逐頁截，避免全部匯出的浪費。回傳 `{0-based-idx: image_path}` dict。含 tqdm 進度條。

`get_clip_path(parsed, page_1based, clips_dir)` — 依標題數字決定子資料夾（有數字 → `clips/058/`，無數字 → `clips/intro/`），回傳完整 clip 路徑。

`create_single_clip(img_path, audio_path, out_path, fps, ffmpeg_path, use_gpu=True)` — 直接呼叫 ffmpeg 合成單頁 MP4。GPU 模式用 `h264_nvenc`（preset p4, tune hq），CPU 模式用 `libx264`（preset veryfast）。加 `-vf scale=trunc(iw/2)*2:trunc(ih/2)*2` 防止奇數解析度錯誤，加 `-profile:v baseline -level 3.0 -pix_fmt yuv420p` 確保 Windows 內建播放器相容。GPU 失敗時自動 fallback 到 CPU。

`create_clips_parallel(todo, image_map, audio_map, parsed_list, clips_dir, fps, ffmpeg_path, use_gpu, max_workers=4)` — 用 `ThreadPoolExecutor` 平行合成多頁，tqdm 進度條顯示完成數/失敗數，回傳 `(done_count, fail_count)`。

`compose_video(clips, output_video, clips_dir, ffmpeg_path)` — 產生 concat_list.txt，用 ffmpeg `-c copy` 無損串接所有 clip（Phase 2 使用）。

---

## settings.yaml 新增參數

原有參數不變，新增以下兩項：

```yaml
# 同時進行的 TTS 請求數上限，建議 3~5，太高會被微軟限流
tts_max_concurrent: 5
```

Phase C 平行數透過 CLI `--workers` 控制，不在 yaml 裡。

---

## 整體流程圖

```
phase1_generate.py
│
├── Phase A：export_slides_to_images()
│   └── 只截目標頁 → {idx: image_path}
│
├── Phase B：generate_audio_for_targets()
│   ├── _generate_page_audio()  ← 每頁
│   │   ├── _generate_one()     ← 每段落（Semaphore 限流）
│   │   └── _concat_audio()     ← 段落+靜音串接
│   └── clips/debug/ log        ← 失敗記錄
│
└── Phase C：create_clips_parallel()
    └── create_single_clip()    ← ffmpeg + GPU（4路平行）
        └── clips/xxx/page_XXXX.mp4

phase2_compose.py
└── compose_video()
    └── ffmpeg -c copy → final_output.mp4
```